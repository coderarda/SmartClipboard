<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SmartClipboard.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartClipboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Settings">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <TextBlock 
                Grid.Column="0"
                Text="Settings" 
                Style="{StaticResource CaptionTextBlockStyle}" 
                VerticalAlignment="Center"
                Margin="16,0,0,0" />
        </Grid>

        <!-- Main Content Area -->
        <StackPanel Orientation="Vertical" Grid.Row="1">
            <!-- Your settings content goes here -->
            <CheckBox x:Name="CheckBox1" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked" Content="Open at startup" Margin="16" />
            <CheckBox x:Name="CheckBox2" Checked="CheckBox2_Checked" Unchecked="CheckBox2_Unchecked" Content="Save history of clipboard" Margin="16" />
            <CheckBox x:Name="CheckBox3" Checked="CheckBox3_Checked" Unchecked="CheckBox3_Unchecked" Content="Minimize when closed" Margin="16" />
        </StackPanel>
    </Grid>
</Window>
