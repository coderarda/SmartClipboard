<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SmartClipboard.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartClipboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SmartClipboard">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- Other content can go here in Row 0 -->
        <ListView Grid.Row="0" Margin="10 25 10 5" x:Name="ClipboardListView" />
        <!-- ListView items can be defined here -->
            <TextBox Grid.Row="1" Margin="20" TextChanged="TextBox_TextChanged" PlaceholderText="Search..." />
    </Grid>
</Window>
